import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zarao.android',
  appName: 'Zarao',
  webDir: './dist/angular_mobile',
  bundledWebRuntime: false
};

export default config;
